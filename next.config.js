/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "nya4nu733jp3xe0c.public.blob.vercel-storage.com",
      },
    ],
  },
};

module.exports = nextConfig;
